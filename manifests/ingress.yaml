apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  creationTimestamp: null
  name: kraft-frontend
spec:
  rules:
  - host: kraft.alexbissessur.dev
    http:
      paths:
      - backend:
          service:
            name: kraft-auth
            port:
              number: 80
        path: /auth
        pathType: Prefix

  - host: kraft.alexbissessur.dev
    http:
      paths:
      - backend:
          service:
            name: kraft-frontend
            port:
              number: 80
        path: /
        pathType: Prefix
status:
  loadBalancer: {}
